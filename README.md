# AI 实践案例报告 Skill

这是一个 Codex Skill，用于把真实 AI 实践整理成结构化案例报告。

## 使用方法

把整个 `ai-case-report` 文件夹放到你的 Codex Skills 目录：

```text
~/.codex/skills/ai-case-report
```

然后在 Codex 新对话中输入：

```text
$ai-case-report
```

它会引导你填写案例信息，判断信息是否完整，必要时追问，并生成最终案例报告。
